/**
 * Generate evaluation report for an employee
 * URL: GET /api/reports/employee/:id
 */
const generateEmployeeReport = async (req, res) => {
  //evaluation report logic for an employee....
};

module.exports = {
  generateEmployeeReport,
};
